/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure experimental.appDir is not needed for Next.js 15+
  // as App Router is the default
  reactStrictMode: true,
}

module.exports = nextConfig 